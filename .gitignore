# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log

# Local session state (already gitignored by convention)
.remember/

# Client invoices — never publish (this repo is the public 4bv.ai site)
invoices/

# Browser-automation scratch output
.playwright-mcp/
wren-home-full.png

# Source OTFs stay local. The site serves woff2 only — a woff2 is not an
# installable desktop binary, which is the point.
design-system/fonts/*.otf
